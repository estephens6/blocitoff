<div class = "list-group">
<%= link_to "", item, method: :delete, class: 'glyphicon glyphicon-ok' %>
    <%= item.name %> -
<!--  <small>submitted <%= time_ago_in_words(item.created_at) %> ago |
  Time left: <%= distance_of_time_in_words(Time.now, item.created_at + 5.minutes, include_seconds: true)%></small>
--></div>
